It is assumed that your system is configured for sudo to gain root privileges.
It is assumed that your system is configured for using non-free packages.

Installer for OpenTox IST/ALU Services
======================================

Usage
-----

tested with Debian Squeeze, might work for Ubuntu and other Debian based distributions

    git clone http://github.com/opentox/install.git
    cd install
    vi config.sh
    ./install


